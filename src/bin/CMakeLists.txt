find_package(Qt5 COMPONENTS Widgets Quick Qml REQUIRED)
add_compile_definitions(QUTIM_PLUGIN_NAME="qutim")
aux_source_directory(src/widgets WIDGET_SRC)
aux_source_directory(src/widgets DECLARATIVE_SRC)
list (APPEND SRC main.cpp ${WIDGET_SRC} ${DECLARATIVE_SRC})
add_executable(qutim "${SRC}")
target_include_directories(qutim PRIVATE "../lib" "${QUTIM_INCLUDE}")
target_link_libraries(qutim Qt5::Widgets libqutim Qtsolutions)
